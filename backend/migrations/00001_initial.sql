-- +goose Up
-- Initial migration placeholder. Schema DDL (profiles, boards, jobs, etc.)
-- will be added in Phase 2 when bounded-context implementations land.
-- This no-op migration establishes the goose version table and verifies
-- that the migration runner can connect to Cloud SQL via IAM auth.

-- +goose Down
-- nothing to roll back
