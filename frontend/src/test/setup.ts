// Side-effect import: extends vitest's expect with DOM matchers (toBeInTheDocument etc.)
// Required for component tests; loaded automatically via vite.config.ts setupFiles.
import '@testing-library/jest-dom';
